-- Phase 0: No schema changes required.
-- HMAC session signing, streaming UI, component decomposition,
-- Gemini message format fix, and configurable context window
-- are all application-layer changes with no database impact.
SELECT 1;
